{
  "name": "demoproject",
  "version": "1.0.0",
  "description": "create app folder create index.html at root install html boilerplate extension create boilerplate html add a script tag to angular (get from google cdn, link is on angularjs.org)",
  "main": "index.js",
  "scripts": {
    "start": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "json-server ./db.json --watch --port 4000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "angular": "^1.6.5",
    "angular-messages": "^1.6.5",
    "json-server": "^0.11.2"
  },
  "devDependencies": {
    "lite-server": "^2.3.0"
  }
}
